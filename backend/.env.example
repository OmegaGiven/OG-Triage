# Copy to .env and adjust as needed. Values here match docker-compose.yml defaults.
DATABASE_URL=postgresql+psycopg://gauge:gauge_dev_password@localhost:5544/gauge_ai_claims

# Used by later phases (extraction / classification / appeal drafting).
ANTHROPIC_API_KEY=
